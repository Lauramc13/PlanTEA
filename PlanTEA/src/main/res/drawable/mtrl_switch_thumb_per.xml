<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- State Checked, Pressed -->
    <item android:color="?grayStroke" android:state_checked="true" android:state_pressed="true"/>

    <!-- State Checked -->
    <item android:color="?colorPrimaryContainer" android:state_checked="true"/>

    <!-- State Unchecked, Pressed -->
    <item android:color="?colorPrimaryContainer" android:state_pressed="true"/>

    <!-- State Unchecked -->
    <item android:color="?colorSurfaceVariant"/>
</selector>
